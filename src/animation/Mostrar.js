import { keyframes } from "styled-components"

export const showUp = keyframes`
   from{
    opacity: 0;
     top:-200px
   }
   to{
    top:0;
   }
`
export const showUpRigth = keyframes`
   from{
    opacity: 0;
     left:-200px;
   }
   to{
    left:10%;
   }
`
export const type = keyframes`
   from{
      width: 0;
   }

`